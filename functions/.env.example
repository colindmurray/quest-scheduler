# Quest Scheduler Functions environment variables
# Copy to .env for local use or .env.<projectId> for deploy.

QS_APP_URL=https://questscheduler.cc
QS_ENC_KEY_B64=base64-encoded-32-byte-key

# OAuth client JSON is stored in Secret Manager (QS_GOOGLE_OAUTH_CLIENT_JSON).
# Local dev can use the JSON file instead.
# Optional: override the OAuth client secret JSON path for local dev.
# QS_GOOGLE_OAUTH_CLIENT_SECRET_FILE=/absolute/path/to/client_secret.json

# Optional fallback (local only) if you are not using the OAuth JSON file.
# QS_GOOGLE_OAUTH_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# QS_GOOGLE_OAUTH_CLIENT_SECRET=your-google-client-secret
# QS_GOOGLE_OAUTH_REDIRECT_URI=https://us-central1-your-project.cloudfunctions.net/googleCalendarOAuthCallback
